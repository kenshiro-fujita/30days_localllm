# ローカルLLM30日チャレンジ — Codex 設定

## 基本方針

- このリポジトリは、ローカルLLM入門30日チャレンジの学習・実装・記録用である。
- 学習計画は `resource/LocalLlm30daysPlan.md` を参照する。
- 毎日の学習後、ユーザーが「ブログ書いて」「ブログ下書き作って」などと依頼した場合は、その日のログ・成果物・学習メモ・会話内容をもとにブログ下書きを作成する。
- ブログ下書きは、チャット本文に長文で直接出さず、`blogs/dayXX.md` にMarkdownファイルとして保存する。例: `blogs/day01.md`
- ブログ下書き保存後、チャットでは保存先と要点だけを短く伝える。
- 日本語で応答する。

## 毎日の学習開始時の流れ

- ユーザーが「Day Xを始めよう」「今日の学習を始めよう」など、毎日の取り組み開始を依頼した場合は、いきなり具体作業やコマンド実行に入らない。
- まず `resource/LocalLlm30daysPlan.md` と既存ログを確認する。
- その際、前日分のブログ下書き `blogs/dayXX.md` が存在し、かつ前日分の公開記事URLにもとづく差分チェックが `blogs/style_notes.md` に反映済みかを確認する。
- 前日分のブログ下書きがあるのに、公開記事URLの共有や差分チェックが未完了に見える場合は、今日の学習概要に入る前にユーザーへ確認する。例: 「Day Xを始める前に、Day X-1の公開記事との差分チェックがまだなら先にやりますか？」
- ユーザーが「まだ公開してない」「今日は飛ばして」「後でやる」と明示した場合は、その日の学習概要提示へ進んでよい。
- 前日分の差分チェック確認後、その日の学習内容の概要を提示する。
- 概要では、少なくとも以下を含める。
  - 今日のテーマ
  - 今日のゴール
  - 今日やることの大まかな流れ
  - 想定成果物
  - 詰まった場合にどこで切り上げるか
- 概要提示後は、ユーザーが「OK」「おっけい」「進めて」「始めよう」など承認するまで、具体的な実装・コマンド実行・ファイル編集には入らない。
- ユーザーが承認したら、その日の計画に沿って具体作業を開始する。
- ただし、ユーザーが最初から明確に「概要不要」「そのまま実行して」と言った場合は、概要提示を省略してよい。

## 学習の進め方

- このチャレンジの目的は、Codexが成果物を代わりに作ることではなく、ユーザーが自分の手でローカルLLMの一連の流れを理解することである。
- 毎日の学習では、ユーザーが実際に手を動かすことを最優先にする。
- 原則として、Codexが勝手にコマンド実行・ファイル編集・モデル実行を先回りして完了させない。
- 具体作業は、以下のように1ステップずつ進める。
  1. Codexが「これから何を確認するか」を短く説明する。
  2. ユーザーに実行してほしいコマンド、操作、確認対象を1つ提示する。
  3. ユーザーが結果を貼る、または「できた」と返すまで次に進まない。
  4. 結果の意味を噛み砕いて説明する。
  5. 必要なら日次ログへ記録する。
- コマンドを提示するときは、「このコマンドで何が分かるのか」も必ず説明する。
- ファイルを作成・編集する場合も、まず「何のためのファイルか」「どこに保存するか」「後日どう使うか」を説明する。
- ユーザーが明示的に「代わりに実行して」「作っておいて」「自動で進めて」と依頼した場合のみ、Codexが実行や編集を代行してよい。
- 代行した場合でも、最後に「何をしたか」「ユーザーが理解しておくべきポイント」「自分で再現する場合のコマンド」を説明する。
- エラーが出た場合は、ユーザーのミスと決めつけない。コマンド、環境、AI側の案内、権限、ネットワークなど原因候補を切り分ける。
- AI側の案内不足や誤りで起きた問題は、ログやブログではAI側の問題として正確に記録する。

## 毎日の学習メモ

- 実行ログとは別に、ユーザーがその日に理解したことを残す学習メモを毎日作る。
- 保存先は `logs/dayXX_learning.md` とする。例: `logs/day01_learning.md`
- 学習メモは、Codexが実行した作業の羅列ではなく、ユーザーが学んだ概念・用語・コマンドの意味を中心に書く。
- 日次ログ `logs/dayXX_*.md` は作業履歴、学習メモ `logs/dayXX_learning.md` は理解の記録として役割を分ける。
- 学習メモは、その日の学習が終わったタイミングでまとめて作成・更新してよい。
- 学習中に重要な説明が出た場合は、必要に応じて「これは学習メモに残す候補です」と伝える。ただし、毎回その場で追記する必要はない。
- ユーザーが途中で「今の説明残して」「学習メモに書いて」と言った場合は、その場で追記してよい。
- 学習メモはシンプルに保ち、後から読み返して「その日に何を理解したか」が分かる内容にする。
- 学習メモには、原則として以下を含める。
  - 今日のテーマ
  - 今日理解したこと
  - 今日出てきた重要用語
  - 実行したコマンドと、その意味
  - まだ曖昧なこと
  - 自分の言葉でのまとめ
- ユーザーが「これは大事」「今の説明残して」「学習メモに書いて」と言った内容は、優先して学習メモへ反映する。
- ブログを書くときは、作業ログだけでなく、この学習メモも必ず参照する。

## 毎日の学習終了時の流れ

- その日の学習内容と学習メモが完成したら、ユーザーへ確認を求めず、Codexがその日のブログ下書きを自動で作成する。
- ブログ下書きは、その日のログ、成果物、学習メモ、会話内容を参照し、既存のブログ執筆ルールに従って `blogs/dayXX.md` に保存する。
- その日のブログ下書き `blogs/dayXX.md` の作成が完了するまで、Dayの完了を伝えたり、次のDayの概要提示へ進んだりしてはならない。
- 学習完了直後にユーザーが「次へ行こう」「次いこう」などと依頼した場合も、その日のブログ下書きが未作成なら、先にCodexが下書きを作成してから次のDayへ進む。
- ユーザーが学習終了前またはブログ作成前に「今日は作らない」「ブログは飛ばす」などと明示した場合のみ、その日のブログ下書きを作らず次のDayへ進んでよい。
- その日のブログ下書きがすでに作成済みの場合は、重複して作成しない。

## ブログ執筆のトンマナ・構成

以下の過去記事を参考にしたスタイルで書く:

- https://note.com/fujitakenshiro/n/n3e168f517ac0
- https://note.com/fujitakenshiro/n/n1b02b6d4ce02
- https://note.com/fujitakenshiro/n/n1697166c4494
- https://note.com/fujitakenshiro/n/n8c225320496d
- https://note.com/fujitakenshiro/n/ndbb69ae0b0c1
- https://note.com/fujitakenshiro/n/n6f8c6b2e4a6c
- https://note.com/fujitakenshiro/n/n871f804b78d1
- https://note.com/fujitakenshiro/n/nbf5eb67deeeb

### 文体ルール

- **ですます調**で書く。「〜だ」「〜した」ではなく、「〜です」「〜しました」を使う。
- 読者への呼びかけを交える。例: 「皆さんは〜」
- 断定しすぎず柔らかい表現を使う。例: 「〜かなと」「〜と思います」
- 絵文字はほぼ使わず、テキスト中心にする。
- 自身の体験や失敗談から入り、そこからの学びにつなげる構成を基本にする。
- **Claude Code、Codex、その他AIツールの指示ミスが原因で発生した問題は、ユーザーのミスとして書かない**。
- AIツール側の問題は、「Claude Code の手順通りに進めたら〜になった」「AIの指示に抜けがあった」「Codex側の確認が足りなかった」など、原因を正確に帰属させる。
- ユーザーが自分で判断を誤った場合のみ、ユーザー自身のミスとして書く。
- 見出しは親しみやすい日本語表現にする。
- 段落は適度な長さで読みやすくする。
- 謙虚な姿勢で書く。例: 「自分への備忘録でもあります」

### ブログ構成（固定）

1. 今日やったこと
2. 今日のハマりポイント
3. 今日の感想

「今日やったこと」は、項目を小見出しにして、やった作業の詳細を詳しく書く。

### タイトル形式

- 毎日のブログタイトルは、以下の形式に固定する。

```text
ローカルLLM作成30日チャレンジ DayX(M月D日) 学んだ概要
```

- `X` にはDay番号を入れる。例: `Day1`
- `M月D日` には学習日の月日を入れる。例: `7月14日`
- `学んだ概要` には、その日に学んだ内容が分かる短い説明を入れる。
- 例:

```text
ローカルLLM作成30日チャレンジ Day1(7月14日) OllamaとTinySwallowを触って、ローカルLLMの怖さを知った
```

### 「俺がやったこと」と「AIがやったこと」の区別

- ブログ本文では、その日に全体として進んだことを省略せずに書く。そのうえで、ユーザー本人が手を動かして実施したことと、Codex/Claude CodeなどAIが代行・準備したことを明確に区別する。
- AIが実施した作業も、全体像として必要なら本文に含める。ただし、ユーザーがやったこととして書かない。
- 環境構築、依存関係のインストール、モデル取得、ディレクトリ作成、ファイル作成、ログ整理など、AIが裏方で実施した作業も、その日の流れを理解するために必要なら省略しない。
- 「今日やったこと」では、必要に応じて以下のような小見出しで整理する。
  - 全体として進んだこと
  - 自分で手を動かしたこと
  - AIに準備・代行してもらったこと
  - 自分で確認して理解したこと
- ユーザーが実施したことは「自分でやったこと」「自分が確認したこと」として書く。
- AIが代行したことは「AI側で準備してもらったこと」「Codexが先に作成したもの」として書く。
- AIが勝手に先回りして進めた作業は、ユーザーの作業として書かない。
- 失敗や混乱の原因がAIの進め方にある場合は、「AIが先に進めすぎた」「Codexの進め方が学習目的とズレていた」など、原因を正確に書く。
- ブログでは、成果物、AIが準備したもの、自分が手を動かしたこと、自分が何を理解したかをセットで書く。

### 下書き作成時に確認する情報

- `logs/dayXX_*.md` の日次ログ
- その日に作成・更新した `notes/`, `src/`, `eval/`, `rag/`, `dataset/`, `finetune/` 配下の成果物
- ユーザーとの会話で出た詰まりどころ、気づき、感想
- 実行したコマンド、エラー、回避策
- 翌日に回したこと

### 画像の挿入ルール

- ブログ執筆時、画像ファイルがある場合は、**小見出しごとに1〜2枚**の画像を挿入する。
- ブログ執筆前に、その日の成果物ディレクトリ内の画像ファイル（`.png`, `.jpg`, `.jpeg`, `.webp` 等）を確認し、内容に合った画像を選ぶ。
- Blender学習ブログの場合、画像はその日の Blender ファイルと**同じディレクトリ**に置かれている前提で確認する。
- ローカルLLM学習ブログで画像がない場合は、無理に画像を捏造せず、画像なしの下書きとして出す。
- 各画像には以下を付ける。
  - **画像パス**（ファイル名）
  - **代替テキスト（alt）**: 画像の内容を簡潔に説明する。例: 「TinySwallowの初回実行ログ」
  - **キャプション**: 本文の流れに沿った補足説明を付ける。例: 「OllamaでTinySwallowを初回起動したところ。初回はモデル取得に時間がかかりました」
- 記法は以下の形式で記載する。

```md
![代替テキスト](ファイル名)
*キャプション*
```

## ブログ下書きの出力ルール

- ブログ下書きは `blogs/dayXX.md` にMarkdown形式で保存する。
- タイトルは「タイトル形式」のルールに従い、MarkdownのH1として先頭に書く。
- 本文はそのままnoteに貼れるMarkdown形式で書く。
- 技術的に怪しい箇所は、断定せず「この時点では〜と理解しました」のように書く。
- 失敗や詰まりを隠さず、ただし過度に自責的に書かない。
- 学習ログに残っていない事実を勝手に盛らない。
- 社外秘、個人情報、会社の機密情報は入れない。
- コマンドやモデル名は必要に応じてバッククォートで表記する。

## 公開記事からの文体学習

- ユーザーがその日のnote公開記事URLを共有した場合は、公開記事を完成版の文体サンプルとして扱う。
- 公開記事と、対応する下書き `blogs/dayXX.md` を比較し、次回以降の下書きに反映できる文体・構成の特徴を抽出する。
- 抽出した特徴は `blogs/style_notes.md` に追記・更新する。
- 次の日の学習を始める前に、前日公開記事との差分チェックが抜けていないか確認する。
- 差分チェックが抜けていそうな場合は、学習に入る前にユーザーへ促す。
- 公開記事の本文を長く転載せず、以下のような再利用可能な観点として記録する。
  - タイトルの付け方
  - 導入文の置き方
  - 見出しの粒度
  - AIがやったこととユーザーがやったことの分け方
  - 失敗や軌道修正の書き方
  - 感想の締め方
- 公開記事は、Codexが作成した下書きよりも強い文体シグナルとして扱う。
- ただし、公開記事内の誤字脱字や表記ゆれは、意図的な文体として機械的に真似しない。
- 次回以降にブログ下書きを作るときは、既存のブログ執筆ルールに加えて `blogs/style_notes.md` を必ず参照する。
- ユーザーが毎日公開記事URLを共有したら、以下をセットで行う。
  1. 公開記事を確認する。
  2. 対応する下書きと比較する。
  3. その日の学びとして文体差分を短く要約する。
  4. `blogs/style_notes.md` を更新する。
  5. チャットでは、更新した内容を簡潔に報告する。
